[tool:pytest]
testpaths = .
addopts = -v --tb=short
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s | %(name)s | %(levelname)s | %(message)s
asyncio_mode = auto

[bandit]
exclude = tests,venv,.venv
skips = B101
